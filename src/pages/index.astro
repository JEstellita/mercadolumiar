---
import BaseLayout from '../layouts/BaseLayout.astro';
import ProgressBar from '../components/ProgressBar.astro';

// Dados padrão durante build - serão atualizados no cliente
const progress = { 
  totalMonthly: 0, 
  goal: Number(import.meta.env.PUBLIC_GOAL_MONTHLY || 150), 
  supporters: 0, 
  stale: true 
};

// Dados dos produtos em destaque (mockup)
const featuredProducts = [
  {
    title: "Hortaliças Orgânicas",
    description: "Verduras frescas direto da horta",
    image: "🥬",
    producer: "Sítio Verde",
    price: "Preço Variável"
  },
  {
    title: "Mel Artesanal",
    description: "Mel puro das montanhas de Lumiar",
    image: "🍯", 
    producer: "Apiário da Serra",
    price: "Preço Variável"
  },
  {
    title: "Queijos Artesanais",
    description: "Queijos frescos e curados",
    image: "🧀",
    producer: "Laticínios do Vale",
    price: "Preço Variável"
  }
];

// Dados dos serviços em destaque (mockup)
const featuredServices = [
  {
    title: "Serviços Culinários",
    description: "Comida caseira e eventos",
    image: "🍽️",
    provider: "Chef da Roça",
    price: "Preço Variável"
  },
  {
    title: "Montagem e Manutenção",
    description: "Computadores e notebooks",
    image: "💻",
    provider: "TechLumiar",
    price: "Preço Variável"
  },
  {
    title: "Transporte Local",
    description: "Transporte de pessoas e encomendas",
    image: "🚗",
    provider: "Transporte Ecológico",
    price: "Preço Variável"
  }
];
---

<BaseLayout title="Mercado Biorregional Lumiar - Compre local, construa futuro">
  <!-- Hero Section -->
  <section class="relative overflow-hidden">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-20">
      <div class="text-center">
        <!-- Hero badge -->
        <div class="inline-flex items-center bg-primary-100 text-primary-800 text-sm font-medium px-4 py-2 rounded-full mb-8">
          <svg class="w-4 h-4 mr-2" fill="currentColor" viewBox="0 0 20 20">
            <path d="M9.049 2.927c.3-.921 1.603-.921 1.902 0l1.07 3.292a1 1 0 00.95.69h3.462c.969 0 1.371 1.24.588 1.81l-2.8 2.034a1 1 0 00-.364 1.118l1.07 3.292c.3.921-.755 1.688-1.54 1.118l-2.8-2.034a1 1 0 00-1.175 0l-2.8 2.034c-.784.57-1.838-.197-1.539-1.118l1.07-3.292a1 1 0 00-.364-1.118L2.98 8.72c-.783-.57-.38-1.81.588-1.81h3.461a1 1 0 00.951-.69l1.07-3.292z"></path>
          </svg>
          Economia Local Sustentável
        </div>

        <!-- Hero title -->
        <h1 class="text-5xl md:text-7xl font-bold text-neutral-900 mb-6 leading-tight">
          Compre local,
          <br>
          <span class="text-transparent bg-gradient-to-r from-primary-600 to-primary-500 bg-clip-text">
            construa futuro
          </span>
        </h1>

        <!-- Hero description -->
        <p class="text-xl md:text-2xl text-neutral-600 mb-8 max-w-4xl mx-auto leading-relaxed">
          Conecte-se diretamente com <strong class="text-primary-600">produtores e prestadores de serviços</strong> 
          em Lumiar e Biorregião. Uma plataforma que fortalece nossa economia local.
        </p>

        <!-- Hero CTAs -->
        <div class="flex flex-col sm:flex-row gap-4 justify-center mb-16">
          <a href="/produtores" class="bg-primary-600 hover:bg-primary-700 text-white px-8 py-4 rounded-xl font-semibold text-lg transition-all duration-200 shadow-lg hover:shadow-xl transform hover:-translate-y-1">
            🛒 Ver Produtores
          </a>
          <a href="/campanha" class="border-2 border-neutral-300 hover:border-primary-300 text-neutral-700 hover:text-primary-600 px-8 py-4 rounded-xl font-semibold text-lg transition-all duration-200">
            📈 Apoiar Campanha
          </a>
        </div>
      </div>
    </div>

    <!-- Background decoration -->
    <div class="absolute inset-0 -z-10">
      <div class="absolute top-20 left-10 w-20 h-20 bg-primary-200 rounded-full opacity-60 animate-pulse"></div>
      <div class="absolute top-40 right-16 w-32 h-32 bg-primary-100 rounded-full opacity-40"></div>
      <div class="absolute bottom-20 left-20 w-16 h-16 bg-primary-300 rounded-full opacity-50"></div>
      <div class="absolute bottom-40 right-10 w-24 h-24 bg-primary-200 rounded-full opacity-30"></div>
    </div>
  </section>

  <!-- Progress Section (mais compacto na home) -->
  <section class="py-16 bg-white">
    <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="text-center mb-8">
        <h2 class="text-3xl font-bold text-neutral-900 mb-4">Progresso da Campanha</h2>
        <p class="text-lg text-neutral-600">Ajude-nos a manter a plataforma funcionando</p>
      </div>
      
      <ProgressBar total={progress.totalMonthly} goal={progress.goal} supporters={progress.supporters} stale={progress.stale} />
      
      <div class="text-center mt-8">
        <a href="/campanha" class="inline-flex items-center text-primary-600 hover:text-primary-700 font-medium">
          Ver detalhes da campanha
          <svg class="w-4 h-4 ml-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
          </svg>
        </a>
      </div>
    </div>
  </section>

  <!-- Products Section -->
  <section class="py-16">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="text-center mb-12">
        <h2 class="text-3xl font-bold text-neutral-900 mb-4">Produtos em Destaque</h2>
        <p class="text-lg text-neutral-600">Direto de quem produz em Lumiar e Biorregião</p>
      </div>

      <div class="grid grid-cols-1 md:grid-cols-3 gap-8 mb-8">
        {featuredProducts.map((product) => (
          <div class="bg-white rounded-2xl shadow-lg border border-neutral-100 overflow-hidden hover:shadow-xl transition-all duration-300 transform hover:-translate-y-2">
            <div class="p-8 text-center">
              <div class="text-6xl mb-4">{product.image}</div>
              <h3 class="text-xl font-bold text-neutral-900 mb-2">{product.title}</h3>
              <p class="text-neutral-600 mb-4">{product.description}</p>
              <div class="border-t border-neutral-100 pt-4">
                <p class="text-sm text-primary-600 font-medium">{product.producer}</p>
                <p class="text-sm text-neutral-500">{product.price}</p>
              </div>
            </div>
          </div>
        ))}
      </div>

      <div class="text-center">
        <a href="/produtores" class="bg-primary-600 hover:bg-primary-700 text-white px-6 py-3 rounded-lg font-medium transition-colors">
          Ver Todos os Produtores
        </a>
      </div>
    </div>
  </section>

  <!-- Services Section -->
  <section class="py-16 bg-white">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="text-center mb-12">
        <h2 class="text-3xl font-bold text-neutral-900 mb-4">Serviços Locais</h2>
        <p class="text-lg text-neutral-600">Prestadores de serviços da nossa comunidade</p>
      </div>

      <div class="grid grid-cols-1 md:grid-cols-3 gap-8 mb-8">
        {featuredServices.map((service) => (
          <div class="bg-gradient-to-br from-primary-50 to-primary-100 rounded-2xl border border-primary-200 p-8 text-center hover:shadow-lg transition-all duration-300 transform hover:-translate-y-2">
            <div class="text-6xl mb-4">{service.image}</div>
            <h3 class="text-xl font-bold text-neutral-900 mb-2">{service.title}</h3>
            <p class="text-neutral-600 mb-4">{service.description}</p>
            <div class="border-t border-primary-200 pt-4">
              <p class="text-sm text-primary-600 font-medium">{service.provider}</p>
              <p class="text-sm text-neutral-500">{service.price}</p>
            </div>
          </div>
        ))}
      </div>

      <div class="text-center">
        <a href="/produtores" class="bg-primary-600 hover:bg-primary-700 text-white px-6 py-3 rounded-lg font-medium transition-colors">
          Ver Todos os Serviços
        </a>
      </div>
    </div>
  </section>

  <!-- How it works -->
  <section class="py-16">
    <div class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="text-center mb-12">
        <h2 class="text-3xl font-bold text-neutral-900 mb-4">Como Funciona</h2>
        <p class="text-lg text-neutral-600">Simples, direto e transparente</p>
      </div>

      <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
        <div class="text-center">
          <div class="w-16 h-16 bg-primary-500 rounded-full flex items-center justify-center mx-auto mb-6">
            <span class="text-white text-2xl font-bold">1</span>
          </div>
          <h3 class="text-xl font-bold text-neutral-900 mb-4">Encontre</h3>
          <p class="text-neutral-600">
            Navegue pelos produtores e prestadores de serviços locais. 
            Veja fotos, descrições e informações de contato.
          </p>
        </div>

        <div class="text-center">
          <div class="w-16 h-16 bg-primary-500 rounded-full flex items-center justify-center mx-auto mb-6">
            <span class="text-white text-2xl font-bold">2</span>
          </div>
          <h3 class="text-xl font-bold text-neutral-900 mb-4">Conecte</h3>
          <p class="text-neutral-600">
            Entre em contato direto via WhatsApp. Tire dúvidas, 
            negocie preços e combine a entrega.
          </p>
        </div>

        <div class="text-center">
          <div class="w-16 h-16 bg-primary-500 rounded-full flex items-center justify-center mx-auto mb-6">
            <span class="text-white text-2xl font-bold">3</span>
          </div>
          <h3 class="text-xl font-bold text-neutral-900 mb-4">Apoie</h3>
          <p class="text-neutral-600">
            Ao comprar local, você fortalece nossa economia 
            e contribui para um futuro mais sustentável.
          </p>
        </div>
      </div>
    </div>
  </section>

  <!-- CTA Section -->
  <section class="py-20 bg-gradient-to-r from-primary-600 to-primary-500">
    <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
      <h2 class="text-3xl md:text-4xl font-bold text-white mb-6">
        Junte-se à nossa comunidade
      </h2>
      <p class="text-xl text-primary-100 mb-8 max-w-2xl mx-auto">
        Ajude-nos a construir uma economia local forte e sustentável. 
        Sua contribuição faz a diferença.
      </p>
      
      <div class="flex flex-col sm:flex-row gap-4 justify-center">
        <a href="/assinar" class="bg-white hover:bg-neutral-100 text-primary-600 px-8 py-4 rounded-xl font-semibold text-lg transition-all duration-200 shadow-lg">
          🌱 Apoiar Mensalmente
        </a>
        <a href="/whatsapp" class="border-2 border-white hover:bg-white text-white hover:text-primary-600 px-8 py-4 rounded-xl font-semibold text-lg transition-all duration-200">
          💬 Falar no WhatsApp
        </a>
      </div>
    </div>
  </section>

  <script>
    // Simular dados de progresso (pode ser conectado a uma API externa depois)
    document.addEventListener('DOMContentLoaded', () => {
      const progressSection = document.querySelector('[data-total]');
      if (progressSection) {
        // Dados simulados - substitua por chamada a API externa se necessário
        const mockData = {
          totalMonthly: 75.50,
          goal: 150,
          supporters: 3
        };
        
        progressSection.setAttribute('data-total', mockData.totalMonthly.toString());
        progressSection.setAttribute('data-goal', mockData.goal.toString());
        progressSection.setAttribute('data-supporters', mockData.supporters.toString());
        
        // Dispatch view_home event
        window.dispatchEvent(new CustomEvent('track', {
          detail: {
            event: 'view_home',
            total: mockData.totalMonthly,
            goal: mockData.goal,
            supporters: mockData.supporters
          }
        }));
      }
    });
  </script>
</BaseLayout>
  </script>
</BaseLayout>